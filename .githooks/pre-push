#!/bin/sh
# Mirror the CI gate locally before pushing, so failures surface here
# instead of on GitHub Actions. Runs: lint -> format:check -> test -> build.
# Bypass with: git push --no-verify
set -e

echo "pre-push: running CI checks (lint, format:check, test, build)..."
npm run ci
echo "pre-push: all checks passed."
